#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int n;
	int a=1;
	scanf("%d", &n);
	for (int i = 1; i >= 1 && i <= n; i++)
	{
	    a  *= i;  //a = a * i;
		if (i == n)
		{
			printf("%d\n", a);
		}
      
	}
	return 0;
}
